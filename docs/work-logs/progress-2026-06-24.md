# 2026-06-24 开发进度

## 今日目标

启动 M1：本地 / 已挂载目录扫描。

本阶段先完成可用的扫描基础能力，并同步调整前端页面框架：

- 支持媒体源配置和目录保存。
- 支持视频文件格式识别。
- 支持全量扫描任务。
- 支持分批扫描，批大小可配置，默认每批 100 个文件。
- 支持批间隔配置，默认每批间隔 1 秒。
- 支持扫描任务状态和扫描结果查看。
- 支持操作日志弹出查看和 TXT 导出。
- 支持页面主题切换：跟随系统、亮色、暗色。
- 后端默认端口调整为 8970。
- 系统默认对外 Web 端口调整为 8971。
- 将项目工作日志统一归档到 `docs/work-logs/`。

## 范围确认

本次“第二阶段”按项目当前进度解释为 M1，而不是里程碑文档中的第二阶段 M4-M6。

M1 不接入 TMDB、DeepSeek、AI 解析、增量扫描和真实重命名。上述能力分别保留到 M4-M6 或后续阶段。

## 页面布局约定

- 页面左侧为可伸缩侧栏。
- 侧栏顶部展示 Logo、标题和操作菜单。
- 侧栏底部展示提示语、系统状态、软件版本、主题切换和退出按钮。
- 页面右侧为操作台区域，点击左侧菜单后切换对应业务界面。
- 操作日志使用弹出页查看，不作为常驻主页面。

## 文档目录调整

工作日志统一放入 `docs/work-logs/`。

已迁移：

- `docs/development/progress-2026-06-23.md` -> `docs/work-logs/progress-2026-06-23.md`
- `docs/development/work-summary-2026-06-23.md` -> `docs/work-logs/work-summary-2026-06-23.md`

## 下一步

## 已完成

- 新增 M1 书面规格：`docs/superpowers/specs/2026-06-24-m1-scan-workbench-design.md`。
- 新增 M1 实现计划：`docs/superpowers/plans/2026-06-24-m1-scan-workbench-implementation.md`。
- 新增 `docs/work-logs/` 并迁移 2026-06-23 工作日志。
- 后端新增扫描配置：
  - `batch_size = 100`
  - `batch_interval_seconds = 1`
- 后端默认端口改为 `8970`。
- 前端 Web 默认端口改为 `8971`。
- 后端新增媒体源、扫描任务、扫描结果和日志 API。
- 后端新增 M1 数据表：
  - `media_sources`
  - `scan_jobs`
  - `media_files`
- 扫描服务支持分批全量扫描，并只保存视频文件。
- 前端新增左侧可伸缩侧栏、右侧操作台布局。
- 前端新增媒体源、扫描任务、扫描结果页面。
- 前端新增日志抽屉和 TXT 导出入口。
- 前端新增主题切换：跟随系统、亮色、暗色。
- 修复 Windows 下 `vite build frontend` 偶发将入口 HTML 作为绝对资源名的问题，改为在 Vite 配置中声明前端 root，并使用 `vite build --config frontend/vite.config.ts`。
- 调整左侧底部信息区：
  - 状态使用小圆点和文字展示。
  - 新增语言切换框，默认中文，功能暂不实现。
  - 版本号和单按钮明暗主题切换放在同一行。
  - 退出入口显示当前用户 `admin`。
- 右侧操作台顶部新增全局搜索框，当前作为入口展示，搜索逻辑后续接入。
- 继续调整侧栏交互细节：
  - 侧栏背景跟随亮色 / 暗色主题切换。
  - 收缩宽度缩小为 58px，图标在收缩区域居中。
  - 收缩后状态区只保留图标，并通过悬浮提示展示名称。
  - 版本号前增加版本类型文字，默认显示为“预览版”。
  - 退出区显示“退出登录”和右侧用户名 `admin`。
  - 修复收缩后折叠按钮隐藏导致无法展开的问题，收缩状态保留居中的展开按钮。

## 已验证

- 后端完整测试通过：`.\.venv\Scripts\python.exe -m unittest discover backend\tests -v`
- 前端 API 测试通过：`npm.cmd run frontend:test`
- 前端主题状态测试通过：`.\node_modules\.bin\vitest.cmd run frontend/src/stores/app.test.ts`
- 前端构建通过：`npm.cmd run frontend:build`
- 本地预览页面已刷新验证：
  - 后端健康状态显示正常。
  - 主题按钮可从亮色切换为暗色。
  - 收缩状态下主题按钮可切回亮色，侧栏背景同步变化。
  - 收缩状态下菜单、状态和退出图标居中显示。
  - 收缩后可通过展开按钮恢复展开状态。
  - 右上全局搜索框已显示。

## 已知事项

- 当前环境未验证 Docker Compose 实际启动。
- 前端构建仍有第三方库 `#__PURE__` 注释警告和包体积警告，不影响 M1 功能。

## 下一步

- 启动本地前后端进行页面联调。
- 在具备 Docker 的环境中验证 `docker compose up --build`。
- 后续 M2 开始标准命名与预览。

## 2026-06-24 10:05 页面视觉补充调整

- 收缩状态下将侧栏收缩按钮调整为独立小把手，避免压住 Logo。
- 修正收缩状态下退出图标偏左的问题，使其与菜单、状态、主题图标保持同轴居中。
- 暗色主题补齐 Element Plus 全局背景、边框、文字和按钮变量。
- 暗色主题下表格标题行、表单、页面头、输入框、抽屉和弹层统一使用深灰层级，避免残留白底黑字。

## 2026-06-24 10:21 收缩按钮样式调整

- 将侧栏收缩按钮从图标按钮改为 CSS 小三角。
- 展开状态三角朝内，用于收起侧栏；收缩状态三角朝外，用于展开侧栏。
- 收缩状态下三角放在 Logo 区域外侧，不遮盖 Logo 和菜单图标。

## 2026-06-24 10:29 侧栏收缩动画

- 为侧栏展开 / 收缩增加 220ms 过渡动画。
- 菜单文字、标题、副标题、提示语、状态文字、版本号和退出区域改为淡入淡出加轻微位移动画。
- 收缩状态下继续保持菜单图标、状态点和退出图标居中。
- 增加 `prefers-reduced-motion` 适配，系统减少动态效果时关闭过渡。

## 2026-06-24 10:32 侧栏回归修正

- 展开状态下退出区域调整为图标和“退出登录”同组靠左，用户名 `admin` 靠右。
- 修复侧栏 `overflow` 裁切导致收缩状态小三角不可见的问题。
- 修复收缩状态下状态图标因隐藏语言框残留间距导致的对齐偏移。

## 2026-06-24 10:37 M1 阶段收尾

- 确认 M1 功能范围已完成：媒体源、分批扫描、扫描任务、扫描结果、日志查看、页面框架、主题切换和默认端口调整均已落地。
- 完成本地验证：
  - 后端测试 12 项通过。
  - 前端 API 测试 5 项通过。
  - 主题状态测试 4 项通过。
  - 前端构建通过。
- 生成 M1 设计手册：`docs/manuals/M1-design-manual.md`。
- 生成 M1 用户手册：`docs/manuals/M1-user-manual.md`。
- Docker Compose 配置已更新，但仍需在具备 Docker 的目标环境执行实机启动验证。

## 2026-06-24 M2 标准命名与预览启动

- 确认 M2 范围：本地文件名解析、标准命名模板、预览记录保存、预览项手动编辑。
- 明确非范围：不调用外部 API，不调用 AI，不执行真实重命名，不做冲突检测。
- 新增 M2 设计规格：`docs/superpowers/specs/2026-06-24-m2-naming-preview-design.md`。
- 新增 M2 实施计划：`docs/superpowers/plans/2026-06-24-m2-naming-preview-implementation.md`。
- 继续遵守工作日志独立存放约定：阶段启动、阶段复盘和验证记录写入 `docs/work-logs/`。

## 2026-06-24 页面问题修复与分页补充

- 修复根布局和三个列表页中损坏的 Vue 模板标签，避免进入“扫描结果”页后侧栏左下状态区被异常 DOM 结构影响而消失。
- 调整收缩状态下侧栏底部状态图标的布局，使状态点与菜单图标、主题按钮、退出图标保持同轴居中。
- 新增通用表格分页组件，媒体源、扫描任务、扫描结果三个列表统一支持分页。
- 分页默认 10 条/页，支持切换 50 条/页和“全部”。
- 新增分页状态测试：`frontend/src/stores/pagination.test.ts`。
- 已验证：
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/pagination.test.ts`
  - `npm.cmd run frontend:test`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/app.test.ts`
  - `npm.cmd run frontend:build`

## 2026-06-24 列表展示规范与侧栏收缩修复

- 新增列表展示格式化工具：文本截断、文件大小自然单位、时间格式化。
- 列表文字列统一左对齐，数字列和日期列统一居中。
- 文件大小展示为 `B / KB / MB / GB / TB` 等自然单位。
- 时间展示为本地自然时间格式，去除毫秒和时区文本。
- 超过 10 个字符的文本显示前 10 个字符加 `...`，鼠标悬浮时通过气泡展示原始完整字符串。
- 气泡内容通过文本插值展示，不使用 HTML 注入，确保特殊字符按原样显示。
- 侧栏收缩时改为不渲染文字节点，避免隐藏文字继续占位挤压图标。
- 收缩状态下菜单、状态、主题、退出图标统一使用固定 42px 图标轨道。
- 已验证：
  - `.\node_modules\.bin\vitest.cmd run frontend/src/utils/displayFormat.test.ts`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/pagination.test.ts`
  - `npm.cmd run frontend:test`
  - `npm.cmd run frontend:build`

## 2026-06-24 列表展示长度配置与排序

- 新增前端列表展示配置：`frontend/src/config/tableDisplayConfig.ts`。
- 文件名配置为完整展示，不再截断。
- 路径展示长度提高到 40 个字符，并保持单行显示，后续可迁移到设置页。
- 媒体源名称、状态、扩展名等展示长度统一从配置读取。
- 新增列表排序状态：`frontend/src/stores/tableSort.ts`。
- 扫描结果页支持按文件大小和修改时间排序。
- 扫描任务页支持按任务 ID、扫描数量、视频数量、警告数、批大小、批间隔、开始时间和结束时间排序。
- 排序在全量列表上执行，再进行分页，避免只排序当前页。
- 已验证：
  - `npm.cmd run frontend:test`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/pagination.test.ts frontend/src/utils/displayFormat.test.ts frontend/src/stores/tableSort.test.ts frontend/src/stores/app.test.ts`
  - `npm.cmd run frontend:build`

## 2026-06-24 列表状态中文与默认排序

- 扫描任务状态由英文转换为中文显示：
  - `pending` -> `等待中`
  - `running` -> `扫描中`
  - `completed` -> `已完成`
  - `failed` -> `失败`
- 扫描任务列表默认按任务 ID 降序排序，并在任务 ID 表头展示默认排序状态。
- 扫描结果列表默认按修改时间降序排序，并在修改时间表头展示默认排序状态。
- 默认排序在全量数据上执行，再进行分页。
- 已验证：
  - `.\node_modules\.bin\vitest.cmd run frontend/src/utils/displayFormat.test.ts frontend/src/stores/tableSort.test.ts frontend/src/stores/pagination.test.ts frontend/src/stores/app.test.ts`
  - `npm.cmd run frontend:test`
  - `npm.cmd run frontend:build`

## 2026-06-24 构建警告整理

- 在前端 Vite 构建配置中增加 Rolldown 日志过滤，仅屏蔽第三方依赖 `@vueuse/core/dist/index.js` 触发的 `INVALID_ANNOTATION` 警告。
- 保留其他构建警告输出，当前仍保留包体积超过默认阈值的提示，后续可通过拆包或调整阈值单独处理。
- 已验证：`npm.cmd run frontend:build` 构建通过，`#__PURE__` 第三方注释警告不再出现。

## 2026-06-24 M2 标准命名与预览实现

- 后端新增本地文件名解析器：支持电影年份、`SxxEyy`、`Season/Episode`、中文“第 N 集”等基础模式。
- 后端新增标准命名模板：电影生成 `{title}.{year}{extension}`，剧集生成 `{title}.SxxEyy{extension}`。
- 数据库新增 `rename_previews` 表，用于保存解析结果、建议文件名、用户编辑文件名和预览状态。
- 后端新增命名预览服务和 API：
  - `POST /api/rename-previews/generate`
  - `GET /api/rename-previews`
  - `PUT /api/rename-previews/{preview_id}`
- 前端新增“命名预览”菜单和页面，支持生成、筛选、统计、查看、排序、分页和编辑目标文件名。
- M2 阶段仍不执行真实文件重命名，不做冲突检测，不调用外部 API 或 AI。
- 生成 M2 文档：
  - `docs/design/M2-design-manual.md`
  - `docs/manuals/M2-user-manual-cn.md`
  - `docs/manuals/M2-user-manual.md`
- 已验证：
  - `$env:PYTHONPATH = "backend"; .\.venv\Scripts\python.exe -m unittest discover backend\tests -v`
  - `npm.cmd run frontend:test`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/app.test.ts frontend/src/stores/pagination.test.ts frontend/src/stores/tableSort.test.ts frontend/src/stores/preview.test.ts frontend/src/utils/displayFormat.test.ts`
  - `npm.cmd run frontend:build`

## 2026-06-24 M2 发布补充与版本打包

- 命名预览页完成表格交互细节调整：
  - 操作按钮移动到筛选条件之后，顺序为“生成预览”“刷新”。
  - 统计数量按状态使用不同颜色展示。
  - 表格列顺序调整为状态、原文件名、目标文件名、解析标题、类型、年份、季/集、更新时间、操作。
  - 表格文本统一按 50 字节截断，悬浮气泡展示完整原始内容。
  - 原文件名和目标文件名支持自然排序。
  - 操作列改为统一图标按钮，悬浮显示操作名称。
- 媒体源页面新增 Windows 服务端本地目录选择器：
  - 新增 `GET /api/media-sources/local-directories` 只读目录浏览接口。
  - 支持从“此电脑”进入盘符、浏览子目录、返回上一级并写回目录路径输入框。
  - 保留手动输入网络路径能力，便于 NAS、Linux 或 Web 访问场景继续使用。
  - 修复从盘符根目录无法返回“此电脑”的问题。
- 版本号更新为 `0.2.0`，用于 M2 正式发布。
- 已验证：
  - `$env:PYTHONPATH = "backend"; .\.venv\Scripts\python.exe -m unittest discover backend\tests -v`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/app.test.ts frontend/src/stores/pagination.test.ts frontend/src/stores/tableSort.test.ts frontend/src/stores/preview.test.ts frontend/src/utils/displayFormat.test.ts frontend/src/utils/localDirectory.test.ts frontend/src/api/client.test.ts`
  - `npm.cmd run frontend:build`

## 2026-06-24 M3 安全重命名启动

- 确认 M3 第一版范围：
  - 只在原目录内修改文件名。
  - 不移动目录。
  - 不覆盖已有文件。
  - 先执行 dry-run 冲突检测，再由用户确认真实重命名。
- 新增 M3 设计规格：`docs/superpowers/specs/2026-06-24-m3-safe-rename-design.md`。
- 新增 M3 实现计划：`docs/superpowers/plans/2026-06-24-m3-safe-rename-implementation.md`。
- 后端新增重命名批次和明细表：
  - `rename_operations`
  - `rename_operation_items`
- 后端新增安全重命名服务：
  - dry-run 检查源文件缺失、目标文件已存在、目标名非法、批次内重复和状态不允许。
  - execute 只处理 dry-run 中 `ready` 项，逐项记录 `renamed` 或 `failed`。
- 后端新增 API：
  - `POST /api/rename-operations/dry-run`
  - `GET /api/rename-operations/{operation_id}`
  - `POST /api/rename-operations/{operation_id}/execute`
- 前端命名预览页新增：
  - 多选预览记录。
  - 冲突检测按钮。
  - 冲突检测结果弹窗。
  - 确认重命名按钮。
  - 执行后刷新预览列表。
- 补齐执行闭环：
  - 重命名成功后同步更新 `media_files.file_path`、`media_files.file_name`、`media_files.extension`。
  - 重命名成功后将对应命名预览状态更新为 `renamed`。
  - 已完成的重命名批次再次执行时直接返回既有结果，避免重复执行和计数回退。
  - 前端命名预览列表支持“已重命名”状态统计和中文展示。
- 本地页面验证：
  - 后端 `8970` 健康检查返回 `0.2.0`。
  - 命名预览页可正常加载 M3 冲突检测入口。
  - 选择需检查记录后 dry-run 弹窗显示冲突，确认重命名按钮保持禁用。

## 2026-06-24 列表查询加载策略调整

- 除媒体源页面外，扫描任务、扫描结果和命名预览页面不再进入页面即加载表格数据。
- 扫描任务页：
  - 初始只加载媒体源下拉选项。
  - 选择媒体源并点击查询后加载该媒体源任务。
  - 支持从媒体源页“查看任务”按钮带媒体源参数跳转并自动查询。
- 扫描结果页：
  - 新增媒体源和扫描任务查询条件。
  - 只有选择扫描任务并点击查询后加载结果。
  - 支持从扫描任务页跳转并自动查询指定任务结果。
  - 后端 `GET /api/media-files` 支持 `media_source_id` 和 `scan_job_id` 过滤。
- 命名预览页：
  - 只有选择扫描任务并点击查询后加载命名预览。
  - 支持从扫描任务页跳转并自动查询指定任务命名预览。
  - 统计栏改为自适应布局，五个状态项保持单行展示。
- 媒体源页新增“查看任务”操作按钮。
- 扫描任务页新增“扫描结果”和“命名预览”操作按钮。
- 已验证：
  - `$env:PYTHONPATH = "backend"; .\.venv\Scripts\python.exe -m unittest discover backend\tests -v`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/app.test.ts frontend/src/stores/pagination.test.ts frontend/src/stores/tableSort.test.ts frontend/src/stores/preview.test.ts frontend/src/stores/renameOperation.test.ts frontend/src/utils/displayFormat.test.ts frontend/src/utils/localDirectory.test.ts frontend/src/api/client.test.ts`
  - `npm.cmd run frontend:build`
  - 浏览器验证扫描结果默认 0 行，带任务参数后只显示该任务结果。
  - 浏览器验证命名预览默认 0 行，带任务参数后加载对应预览，统计栏不换行。
- 补充调整：
  - 扫描任务、扫描结果和命名预览页面如果已有查询数据，通过菜单再次进入时不主动清空列表。
  - 只有用户清空条件、切换媒体源或重新查询时才更新列表。
  - 已验证：`npm.cmd run frontend:build`、前端相关 Vitest 用例通过。

## 2026-06-24 命名预览查询栏布局修正

- 命名预览页查询栏拆分为查询条件组和操作按钮组，避免筛选项和按钮在窄宽度下相互挤压、窜行。
- 操作按钮组在空间不足时作为整体换行展示，查询条件保持稳定的换行间距。
- 已验证：`npm.cmd run frontend:build` 通过；浏览器刷新命名预览页后，当前窄视口下无横向溢出，查询条件和按钮分组展示正常。

## 2026-06-24 今日收工记录

- M3 第一阶段已完成安全重命名 dry-run、执行接口、前端冲突检测弹窗和命名预览页批量操作入口。
- 已完成列表查询加载策略调整：除媒体源页外，扫描任务、扫描结果和命名预览页均改为手动查询后加载，已有查询结果通过菜单返回时保留。
- 已整理 M3 设计规格和实现计划，统一放入 `docs/superpowers/specs/` 和 `docs/superpowers/plans/`。
- 已验证：
  - `$env:PYTHONPATH = "backend"; .\.venv\Scripts\python.exe -m unittest discover backend\tests -v`
  - `.\node_modules\.bin\vitest.cmd run frontend/src/stores/app.test.ts frontend/src/stores/pagination.test.ts frontend/src/stores/tableSort.test.ts frontend/src/stores/preview.test.ts frontend/src/stores/renameOperation.test.ts frontend/src/utils/displayFormat.test.ts frontend/src/utils/localDirectory.test.ts frontend/src/api/client.test.ts`
  - `npm.cmd run frontend:build`
