# 2026-07-12 M10 v0.10.7 正式发布

## 发布范围

- M10 多站点元数据源正式发布：Bangumi、TVDB、用户自备豆瓣代理、多源匹配编排、候选合并与字段来源展示。
- 延续 M6 敏感词和外部提交保护，所有多源调用仍经过拦截和覆盖审计。
- 延续 M8 模板版本快照与 M9 权限、审计、任务治理能力。

## 文档同步

- README 更新最近正式发布版本和当前阶段。
- M10 开发计划补齐 M10-6 完成状态。
- 总设计和项目设计将 M10 更新为已发布，并登记 M11 规划状态。
- 新增 M11 远程协议扩展开发计划，明确 WebDAV、SFTP、S3 和 FTPS / FTP 的顺序、边界与安全约束。

## 发布验证

- `npm.cmd run backend:test`：通过，272 项测试。
- `npm.cmd run frontend:test`：通过，14 个测试文件、74 项测试。
- `npm.cmd run frontend:build`：通过；保留既有分块体积警告。
- `npm.cmd run check:encoding` 与 `git diff --check`：通过。
- `npm.cmd run release:package`：生成 `releases/mediaai-renamer-frontend-v0.10.7.zip`，共 5 个条目，仅包含 `config/config.example.toml`，未包含运行时 `config.toml`。
- 本机未安装 Docker，未执行两份 Compose 配置校验；镜像构建与发布继续由 `v0.10.7` 标签触发的 GitHub Actions 负责。

## 发布结果

- `main` 合并提交：`3c7dd21 release: 发布 v0.10.7`。
- 已推送带注释标签 `v0.10.7`，GitHub Release：[v0.10.7](https://github.com/txlfire/mediaai-renamer/releases/tag/v0.10.7)。
- 发布资产：`mediaai-renamer-frontend-v0.10.7.zip`，`485593` 字节，SHA-256：`a9a8440df7b1ce4880a28c2745bc73b2d31c840499f16aa850ce95b1714f39ed`。
- 标签与 main 推送均已触发 Docker GHCR Publish；前后端镜像构建和推送成功。
- GitHub Actions 当前报告 Node.js 20 依赖弃用提示，流程成功但后续应升级 Actions 运行时声明。
