# v1.0.1 单容器实机发布记录

日期：2026-07-31

## fnOS 环境

- 主机：`192.168.50.43`
- Docker Server：`28.5.2`
- 架构：`x86_64`
- 部署目录：`/vol1/1000/docker/mediaai-renamer`

## 迁移过程

1. 备份旧数据库、配置、日志、`.env` 和双容器 Compose。
2. 停止并删除旧前端、后端容器及对应 `v1.0.0` 镜像。
3. 上传当前已提交源码，显式排除正式 `config.toml`、数据和日志。
4. 使用 `docker/Dockerfile` 完成前端、后端多阶段单镜像构建。
5. 启动 `mediaai-renamer` 单容器并复用原持久化目录。
6. 恢复 fnOS 原 Docker daemon 配置并执行容器重启验收。

## 验收结果

- 后端测试：325 项通过，10 项 WebDAV 受控集成测试跳过。
- 前端测试：18 个测试文件、82 项测试全部通过。
- 前端生产构建和编码检查通过。
- npm 安全审计：0 个已知漏洞。
- 统一 ZIP：
  - 文件：`releases/mediaai-renamer-v1.0.1.zip`
  - 大小：640641 字节
  - SHA-256：`6D13D41BD3C5563CFF18712B4C1728DB1E6A677C744DB699D932F5E2D3DE7494`
  - 78 个条目，正式配置、数据、日志、测试和缓存检查为空。
- ZIP 解压后单进程启动成功，首页、健康接口和 SPA 路由均返回 200。
- `docker compose config --services` 只返回 `mediaai`。
- `8970/api/health` 和 `8971/api/health` 均返回 200。
- 两个端口的首页、JS、CSS 和 `/settings` SPA 路由均返回 200。
- 缺失 JS 静态资源返回 404。
- Docker daemon 重启和容器重启后服务恢复正常。
- 镜像环境仅包含应用运行变量，不包含 HTTP/HTTPS 构建代理。
- 最终源码镜像：`mediaai-renamer:local`，镜像 ID 前缀 `5f3231b13bde`。
- 旧 NAS 数据库本身为空库，迁移后表结构正常，未发生用户数据丢失。

旧部署完整备份保存在：

```text
/vol1/1000/docker/mediaai-renamer-backup-20260731-093706.tar.gz
```

## 环境问题

fnOS 原 `docker.fnnas.com` 镜像站对官方基础镜像返回 401。实测采用临时
Docker daemon 代理拉取基础镜像，并通过预定义构建参数下载 npm/pip 依赖；
构建完成后已恢复原 daemon 配置。
