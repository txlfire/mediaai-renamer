# 2026-06-27 系统设计回溯与汇总

## 本次目标

- 回溯系统设计文档、阶段设计文档和开发计划。
- 检查当前阶段顺序和已完成能力是否存在偏差。
- 补全系统设计文档。
- 生成一份统一总设计文档，作为后续开发的全局入口。

## 发现的偏差

- 早期总设计中将 Bangumi、豆瓣、TVDB 写入匹配流程，但当前只真实落地 TMDB；多站点后移到 M9。
- 早期文档将 AI / DeepSeek 放在 M5；当前已调整为 M5 先做 NAS / SMB / NFS 共享目录，AI 后移 M6。
- M4 Future 中包含较多高级命名规则设计，但当前未落地；统一后移到 M7。
- M4 文档提到管理员权限拦截，但当前没有登录和权限体系；统一后移到 M8。
- 增量扫描多次出现，但没有独立里程碑；后续可在 M5 共享目录稳定后或 M8 运维增强阶段评估。

## 本次文档更新

- 重写 `docs/design/project-design.md`，对齐当前 M0-M9 阶段路线。
- 新增 `docs/design/MediaAI-Renamer-总设计文档.md`，汇总产品目标、阶段完成度、偏差修正和后续推荐顺序。
- 更新 README 文档索引，增加系统设计文档和总设计文档入口。
- 更新 `docs/development/development-guide.md`，新增阶段收尾设计一致性检查要求：每个阶段完成后必须比对总设计、阶段设计、开发计划和验收清单；如有偏差先补全文档，并在工作日志和收尾汇报中说明。
- 新增 `docs/manuals/MediaAI-Renamer-用户手册.md`，汇总 M1-M4 已完成功能的当前完整用户手册。
- 更新 `docs/development/development-guide.md`，新增阶段完成后必须同步更新阶段用户手册和总用户手册的规范。
- 更新 README 文档索引，增加当前完整用户手册入口。
- 根据 M5 新补充计划，正式将 M5 范围调整为 NAS / SMB / NFS 共享目录支持；同步更新总设计、系统设计、README、正式 M5 开发计划和正式 M5 验收清单。
- 明确 M5 引入 SMB 凭据加密模块，凭据用于连接测试和共享访问校验；NFS 不保存凭据，只校验本地挂载点。
- 明确 M5 新增任务状态 `partial_completed` 和 `connection_lost`，并补充旧状态兼容和未知状态兜底展示要求。
- 补充网络协议能力边界：M5 支持本地、SMB、Linux / NAS / Docker 挂载路径和 NFS 本地挂载点；WebDAV、FTP / SFTP、S3 调整为后续候选能力，并要求在界面和文档中向用户明确说明能力差异。
- 补充 M5 共享协议接口架构要求：本地、挂载路径、SMB、NFS 必须通过统一协议接口接入；系统设置展示协议配置和说明；媒体源列表展示连接协议；WebDAV、FTP / SFTP、S3 先作为后续候选协议能力展示。
- 新增 `docs/superpowers/plans/2026-06-27-m5-shared-protocol-interface-implementation.md`，作为后续 M5 协议接口实现计划。
- 将本项目阶段文档治理流程沉淀为本地 Codex skill：`project-docs-governance`，用于后续项目复用阶段收尾、设计偏差检查、用户手册汇总、README 和工作日志同步流程。
- 扩展 `project-docs-governance` skill 范围，覆盖设计文档、开发规范、项目计划、阶段计划、实现计划、验收手册与清单、用户手册等文档的编写和复核。
- 复查项目文档覆盖范围后，继续扩展 `project-docs-governance` skill：补充产品/开发方案、技术选型评估报告、Agent 指令规范、常用脚本任务文档、依赖与环境清单的编写和复核要求。
- 更新 `project-docs-governance` skill 触发描述，补充中文触发词，包括整理文档、复核文档、更新设计文档、更新开发规范、整理项目计划、整理阶段计划、编写验收清单、汇总用户手册、阶段收尾、同步 README、记录工作日志和检查设计偏差。
- 根据 M5 凭据边界补充要求，更新正式 M5 计划、正式验收清单、M5 协议接口实现计划和旧版补充文档：NFS 类型不展示用户名、密码输入框，不存储密码类凭据，日志不得输出密钥类信息；仅 Windows UNC / SMB 类型展示账号、密码输入框；M5 不开发全局独立凭据管理模块，SMB 账号密码仅复用媒体源自身加密字段保存；M5 暂不支持 NFS Kerberos 域认证、keytab、principal、realm 等配置和密钥存储。
- 根据 M5 落地设计建议，进一步统一路径类型枚举为 `local / unc / mounted_nfs`：`unc` 才展示账号和密码并加密入库；`local` 和 `mounted_nfs` 隐藏账号密码控件；NFS 作为 `mounted_nfs` 已挂载路径场景处理，仅记录 IP、导出路径、版本和挂载选项等说明字段，不保存密码字段。同时补充独立凭据管理模块评估，结论为 M5 暂不建设，待 WebDAV / FTP / SFTP / S3 等协议正式接入并出现跨媒体源复用、Token 刷新、密钥轮换或审计需求后再单独设计。

## 当前后续路线

1. M5：NAS / SMB / NFS 共享目录支持，系统设置非 TMDB 分类落地。
2. M6：DeepSeek / AI 智能解析。
3. M7：命名规则高级配置。
4. M8：权限、历史、回滚和审计。
5. M9：多站点元数据源。
