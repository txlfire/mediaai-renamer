# M7-0 增量扫描基础结构记录

日期：2026-07-03

## 本阶段范围

- 新增增量扫描索引表 `scan_file_index`。
- 扩展 `scan_jobs`，增加扫描模式和增量统计字段。
- 新库初始化和旧库迁移统一升级到 schema version 11。
- 保持旧 API 兼容，未传扫描模式时数据库默认使用 `full`。
- 补充 M7 上级文件夹标题识别模式和 AI Provider / 模型切换预留接口设计。

## 版本

- 开发版本由 `0.7.0` 升级为 `0.7.1`。

## 后续

- M7-1 将在该结构基础上实现全量扫描写索引、增量扫描比对和任务摘要统计。
