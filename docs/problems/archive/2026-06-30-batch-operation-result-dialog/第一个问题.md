请修改批量操作（TMDB匹配、全部TMDB匹配、重命名、全部重命名等）的进度和结果展示方式：



1\. 将当前的页面嵌入式进度日志组件改为弹窗方式展示操作结果。



2\. 操作执行中：在页面顶部操作按钮下方显示一个紧凑的进度条（高度约4-6px），同时在按钮上显示加载状态和百分比文字（如“匹配中 45%”），不占用大量页面空间。



3\. 操作完成后：自动弹出结果弹窗，弹窗内容包括：

&#x20;  - 操作标题（如“TMDB匹配完成”）

&#x20;  - 摘要统计（成功X个，失败X个，跳过X个）

&#x20;  - 详细日志列表（可滚动查看，显示每个文件的匹配结果）

&#x20;  - “查看完整日志”按钮（跳转或展开）

&#x20;  - “关闭”按钮



4\. 弹窗宽度建议600-700px，高度自适应，最大高度为视口的60%，超出时内部滚动。



5\. 关闭弹窗后，页面表格自动刷新，显示操作后的最新数据。



6\. 历史记录功能暂不实现，后续如有需求在专门的“操作日志”菜单中统一提供。



7\. 移除当前在页面上直接展示进度和结果日志的组件，避免布局拥挤和错乱。

