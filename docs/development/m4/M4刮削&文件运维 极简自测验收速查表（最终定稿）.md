# M4刮削\&文件运维 极简自测验收速查表（最终定稿）

**核心原则**：存量功能零损坏、新增功能全闭环、热配置可实时生效、所有操作不破坏源文件、全场景可降级无报错

## 一、热配置与参数校验

- **热生效**：TMDB配置、站点开关、文件最小扫描阈值等全部支持热更新，无需重启服务

- **配置优先级**：环境变量 \> 数据库热配置 \> 默认兜底值

- **参数校验**：文件阈值/超时时间/语种/开关参数严格校验，非法值自动兜底、不生效、不阻塞任务

- **安全合规**：密钥前端脱敏、日志脱敏，配置变更留存管理员操作日志

## 二、文件过滤 \& 双列表分流核心校验

### 2\.1 尺寸过滤规则

- 低于配置阈值文件：**不进入解析/刮削/重命名队列**，仅归入待处理列表，标记【尺寸过滤】

- 阈值配置异常自动兜底 0Byte，默认放行全部文件

### 2\.2 无效文件筛选

- 解析残缺、无有效影视信息、花絮、预告片、片段文件，自动归入待处理列表，标记【无需刮削】

- 筛选逻辑零侵入原有本地解析存量功能

### 2\.3 双列表隔离（强校验）

- 正常刮削预览列表 / 待处理运维列表 **完全数据隔离**，无交叉、无错乱、无数据遗漏

## 三、待处理列表运维功能（数据安全核心）

所有操作**仅修改任务记录，不删除、不改动本地源文件**

- **单条移除**：单独删除单条任务记录，重新扫描可恢复队列

- **一键清空**：批量清理列表任务，带二次确认弹窗，无文件操作

- **文件迁移**：支持单选/批量勾选，自定义目录迁移文件

- **容错机制**：单文件迁移失败不影响批量任务，失败文件留存并标注异常

- **自动刷新**：重新扫描增量更新列表，空状态展示正常

- **状态同步**：迁移成功文件自动移出列表，失败文件保留待处理

## 四、全局固定执行时序（禁止颠倒）

目录扫描 → 尺寸阈值过滤 → 本地规则解析 → 无效文件二次筛选 → 多站点刮削\(可选\) → 统一匹配度打分 → 择优决策 → 双列表分流渲染

## 五、多站点刮削架构校验

- TMDB为主站，预留第三方刮削站点拓展接口，架构可无缝拓展

- 单站点异常/超时/限流独立隔离，不阻塞整体刮削流程

- 全站点统一入参、出参、打分规则，支持分值横向择优排序

- 全站点失效自动降级纯本地解析，无报错、不中断任务

- 支持单站开关、优先级配置，热配置实时生效

## 六、匹配度加权算法（精准可编码）

### 6\.1 固定权重（不可修改）

标题相似度60分 \+ 年份精度20分 \+ 媒体类型10分 \+ 季集匹配10分（电影季集维度默认满分兜底）

### 6\.2 计分规则

- 标题：中英双语择优取最高相似度分值

- 年份：一致20分、差1年15分、差2年及以上0分

- 类型不匹配直接0分，杜绝影视错配

- 总分强制兜底 0\-100区间，无负数、无超分

### 6\.3 分值状态映射（全局唯一）

- ≥85分：高置信 → 自动回填标准元数据

- 30\~84分：中低置信 → 弹窗人工择优

- 0\~29分/接口异常：极低置信 → 降级本地、标记需检查

## 七、前端交互校验

- 配置页：所有参数可编辑、热保存、实时生效

- 预览页：展示匹配度、来源站点、匹配状态

- 低置信场景正常唤起候选弹窗，信息展示清晰

- 列表标签区分明确，运维按钮交互、二次确认逻辑正常

## 八、存量兼容 \& 异常兜底（必过项）

- 关闭所有刮削功能可完全回退旧版本地解析能力，存量功能无异常

- 新增功能均为后置增强，零侵入原有扫描、清洗、重命名核心逻辑

- 无密钥、网络异常、站点宕机、文件异常等全场景均可正常降级，无报错、无任务中断、无数据损坏

> （注：部分内容可能由 AI 生成）
