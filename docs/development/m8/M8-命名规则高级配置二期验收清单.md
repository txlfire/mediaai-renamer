# M8 命名规则高级配置二期验收清单

## 0. 验收口径

- M8-0 到 M8-3 已完成主要开发，M8-4 负责文档、验收和发布前校验。
- 验收状态以 2026-07-08 本地自动化验证和文档核对为准。
- 历史预览只提示版本状态，不自动改写目标文件名。
- 用户手动编辑过的目标文件名必须继续受到重新生成保护。

## 1. 配置兼容

- [x] 旧命名模板配置可直接读取，不因新增版本字段报错。
- [x] 旧配置保存后会补齐版本信息和更新时间。
- [x] 导入旧格式模板时可给出兼容处理或明确错误提示。

## 2. 模板导入导出

- [x] 可导出当前电影模板和剧集模板配置。
- [x] 导入模板时会校验 JSON 结构、字段定义和格式参数。
- [x] 非法模板不会覆盖当前生效配置。

## 3. 规则测试与差异预览

- [x] 用户可输入样例并查看模板生成结果。
- [x] 测试结果会展示字段命中情况和格式警告。
- [x] 保存前可对比当前模板结果与待保存模板结果差异。
- [x] 差异预览不会改写正式命名预览记录。

## 4. 双语命名

- [x] 电影模板可配置双语字段组合方式。
- [x] 剧集模板可配置双语字段组合方式。
- [x] 缺少中文标题或英文标题时能自动跳过空字段和多余分隔符。

## 5. 历史预览联动

- [x] 新生成的命名预览会记录模板版本号。
- [x] 新生成的命名预览会记录模板类型和模板更新时间快照。
- [x] 模板更新后，历史预览可提示基于旧模板生成。
- [x] 旧库中没有模板版本快照的历史预览可正常展示，并明确提示版本未知。
- [x] 旧模板提示不阻断编辑、TMDB 匹配、AI 匹配、dry-run 和安全重命名。
- [x] 对手动编辑过的目标文件名，重生成前仍有明确确认提示。
- [x] 候选确认回填目标名时不会丢失模板版本状态。

## 6. 回归

- [x] M2 命名预览基础流程正常。
- [x] M3 dry-run 和安全重命名正常。
- [x] M4 TMDB / IMDb 回填后命名构建正常。
- [x] M6 AI 候选回填后命名构建正常。
- [x] M7 标题识别模式和手动排除正常。

## 7. 自动化验证

- [x] `npm.cmd run backend:test` 通过。
- [x] `npm.cmd run frontend:test` 通过。
- [x] `npm.cmd run frontend:build` 通过。
- [x] `npm.cmd run check:encoding` 通过。
- [x] `git diff --check` 通过。
